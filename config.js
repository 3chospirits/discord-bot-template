module.exports = {
    bot_admins: [""], //put the "admins" of the bot here, this will give them access to commands marked with `adminOnly: true`
    token: "", //bot's login token from the discord developer page
    prefix: "." //prefix that the command calls should start with
}
//any changes to this file will require a full bot restart to update 