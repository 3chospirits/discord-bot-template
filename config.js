module.exports = {
    bot_admins: ["your_discord_id"], //put the "admins" of the bot here, this will give them access to commands marked with `adminOnly: true`
    token: "your_bot_token", //bot's login token from the discord developer page
    prefix: "." //prefix that the command calls should start with
}
//any changes to this file will require a full bot restart to update 